//my other outcomes but might have defects
//film Title output
let findTitle = films.find((film) => {
  return film.title[0];
});
title.innerHTML = JSON.stringify(findTitle);

.then((response))

//const index = moviesData.findIndex((m)=>m.id === currentMovie.id
//we use the if consition to specify the movie id if the movie id deleted doesnt exist we will get a error
/* if(index!== -1){
  movieData.splice(index,1 )
} */

